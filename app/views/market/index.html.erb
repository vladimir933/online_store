<!DOCTYPE html>
<html>
  <body>
    <%= render partial: 'plug_in/navigation' %>
    <%= button_to 'Добавить товар', new_product_path, method: :get %><br>
    <table>
      <tbody>
        <% @products.each do |p| %>
          <tr>
            <td><%= button_to p.name, product_path(p), method: :get %></td>
            <td><%= link_to 'Изменить', edit_product_path(p), method: :get %></td>
            <td><%= link_to 'Удалить', p, method: :delete %></td>
          </tr>
          <tr>
            <td>
              Описание товара:
              <%= p.description %><br>
              Цена:
              <%= p.price / 100 %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </body>
</html>